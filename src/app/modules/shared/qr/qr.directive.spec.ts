import {QrDirective} from './qr.directive';

describe('QrDirective', () => {
  it('should create an instance', () => {
    const directive = new QrDirective();
    expect(directive).toBeTruthy();
  });
});
